name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: install build deps
      run: |
        sudo apt-get update
        sudo apt-get install makepkg
